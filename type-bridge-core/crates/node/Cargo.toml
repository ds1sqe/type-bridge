[package]
name = "type-bridge-node"
version = "2.1.0"
edition = "2024"
description = "TypeScript and Node SDK bindings for the TypeBridge shared Rust semantic engine"
license.workspace = true
license-file.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lib]
name = "type_bridge_node"
crate-type = ["cdylib", "rlib"]

[features]
default = []
contract-test-adapter = []

[dependencies]
type-bridge-contract = { path = "../contract", version = "2.1.0" }
type-bridge-core-lib = { path = "../core", version = "2.1.0" }
type-bridge-orm = { path = "../orm", version = "2.1.0", default-features = false, features = ["band8", "band9"] }
napi = { version = "2", features = ["napi6"] }
napi-derive = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["rt-multi-thread"] }

[build-dependencies]
napi-build = "2"

[lints]
workspace = true
