[package]
name = "type-bridge-schema-migration"
version = "1.5.11"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
type-bridge-contract = { path = "../contract" }
type-bridge-query = { path = "../query" }
type-bridge-schema = { path = "../schema" }

[lints]
workspace = true
