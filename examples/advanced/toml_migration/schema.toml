# Frozen read-only TOML conversion fixture. Do not use as active schema authority.

[attributes.customer-name]
value = "string"

[entities.customer]
owns = [
    { attribute = "customer-name", key = true },
]
